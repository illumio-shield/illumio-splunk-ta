[<stanza name>]

action.markquarantine = [0|1]
   * Enable stream action
    
action.markquarantine.param.workload_uuid = <string>
   * workload_uuid to quarantine

action.markquarantine.param.host = <string>
   * host in the incident

action.markquarantine.param.orig_host = <string>
   * orig_host in the incident